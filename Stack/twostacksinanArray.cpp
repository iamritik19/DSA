#include<iostream>
using std::cout;
using std::endl;
class 2stacksinanArray{
   public:
   	int *arr;
   	int size;
   	int idx1;
   	int idx2;
   	2stacksinanArray(int size){
   		this->size=size;
   		idx1=-1;
   		idx2=size;
   		this->arr=new int[size];
   	}
   	int push1(int data){
       if(idx2-)
   	}
};